{
  "name": "passerby-memories",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --prefix client"
  },
  "dependencies": {
    "mongodb": "^3.6.3"
  }
}
